# Optional live-apply credentials.
# The application does not auto-load this file; export the variables in your shell
# or source it explicitly before running a live-apply workflow.

export JOBFLOW_GREENHOUSE_API_KEY="replace-me"
export JOBFLOW_LEVER_API_KEY="replace-me"
